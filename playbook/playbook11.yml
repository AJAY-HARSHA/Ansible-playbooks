---
- name: implenting world scope variables
  hosts: all
  tasks:
    - name: creating user
      user:
        name: "{{a}}"
        password: "{{b}}"
        home: "{{c}}"
    - name: creating file in users home dir
      file:
        name: "{{d}}"
        state: "{{e}}"
...
